Strings in Python:

In this chapter, I learned about Strings in Python, which are used to store and manipulate text data.
All concepts were learned and practiced by following the CodeWithHarry Python course.

**Topics Covered:

* What is a String in Python

*Creating strings using:

Single quotes (' ')

Double quotes (" ")

Triple quotes (''' ''')

* String Indexing

* String Slicing

Positive indexing

Negative indexing

**String Functions:

len()

upper()

lower()

strip()

replace()

find()

count()

** String immutability (strings cannot be changed)
